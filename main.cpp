#include <iostream>
#include <cmath>

using namespace std;

int main() {

  int N, C;
  cin >> N;
  cin >> C;
  int sort[N];
  int numbers[C]; // tracks frequency of a specific number in sort[]

  // for loops
  for(int i = 0; i < N; i++)
    {
      cin >> sort[i];

  
  }
  for(int i = 0; i < N; i++)
  {
    for(int i = 0; i < C; i++)
  {
    if (sort[N] == C){ // checks if number in sort[] equals C
      numbers[C - 1]++;

      }
    
    }

  }

}
